{
  "name": "repairmate",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "build:frontend": "npm --prefix apps/frontend run build",
    "dev:backend": "uv run --project apps/backend uvicorn app.main:app --reload --port 8000"
  }
}
