CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://repair365mate.vercel.app
# This project's Vercel preview/branch deploys are allowed by default.
# Override to widen/narrow the pattern; leave unset to use the default.
CORS_ALLOW_ORIGIN_REGEX=https://repair365mate-[a-z0-9-]+\.vercel\.app
